apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-web
spec:
  replicas: 5
  template:
    spec:
      containers:
        - name: web
          env:
            - name: LOG_LEVEL
              value: warn
            - name: DB_POOL_SIZE
              value: "50"
