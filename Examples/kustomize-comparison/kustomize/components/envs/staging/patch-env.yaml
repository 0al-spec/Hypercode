apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-web
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: web
          env:
            - name: LOG_LEVEL
              value: info
