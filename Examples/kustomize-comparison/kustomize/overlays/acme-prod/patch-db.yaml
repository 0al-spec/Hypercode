apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-web
spec:
  template:
    spec:
      containers:
        - name: web
          env:
            - name: DB_HOST
              value: db.acme.internal
            - name: DB_POOL_SIZE
              value: "80"
